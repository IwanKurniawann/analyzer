name: 🤖 Trading Bot - Pivot Point SuperTrend

on:
  schedule:
    # Run every 15 minutes
    - cron: '*/15 * * * *'

  workflow_dispatch:
    # Allow manual trigger

jobs:
  trading-analysis:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Checkout repository
      uses: actions/checkout@v4

    - name: 🐍 Set up Python 3.12
      uses: actions/setup-python@v4
      with:
        python-version: '3.12'

    - name: 📦 Cache dependencies
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: 🧪 Install and Verify Dependencies
      run: |
          # 1. Install dependencies first
          pip install --no-cache-dir -r requirements.txt
          
          # 2. Then, verify the installation
          echo "Verifying installations..."
          python -c "import pandas_ta as ta; print(f'✅ pandas-ta version: {ta.version}')"
          python -c "import ccxt; print(f'✅ ccxt version: {ccxt.__version__}')"

    - name: 🚀 Run trading analysis
      env:
        # KuCoin API Configuration
        KUCOIN_API_KEY: ${{ secrets.KUCOIN_API_KEY }}
        KUCOIN_API_SECRET: ${{ secrets.KUCOIN_API_SECRET }}
        KUCOIN_PASSPHRASE: ${{ secrets.KUCOIN_PASSPHRASE }}
        KUCOIN_SANDBOX: "True"

        # Telegram Configuration
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

        # Trading Parameters
        TRADING_PAIRS: "BTC/USDT,ETH/USDT,SOL/USDT,ADA/USDT,SOL/USDT"
        TIMEFRAME: "1h"
        PIVOT_PERIOD: "2"
        ATR_FACTOR: "3.0"
        ATR_PERIOD: "10"

        # Settings
        LOG_LEVEL: "INFO"
        ENABLE_NOTIFICATIONS: "True"

      run: |
        python main.py

    - name: 📋 Upload logs (if failed)
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: trading-bot-logs
        path: trading_bot.log
        retention-days: 7
